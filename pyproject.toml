[project]
name = "devcontainer-playground"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tony Klein", email = "mergemartialarts@tony-klein.de" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
devcontainer-playground = "devcontainer_playground:main"

[build-system]
requires = ["uv_build>=0.10.0,<0.11.0"]
build-backend = "uv_build"

# Exclude dev dependencies from default sync
# Use 'uv sync --dev' to install development tools
[tool.uv]
default-groups = []